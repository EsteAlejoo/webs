# TODO

## Skills de diseño para agentes de IA — pendientes de instalar

Candidatos evaluados para mejorar las decisiones de diseño/UI/animación al
trabajar en este proyecto con agentes de IA (Claude Code y similares). Aún
no instalados — falta decidir dónde/cómo integrarlos.

- **emilkowalski/skill** — Skills de animación y diseño UI (por el autor de
  Sonner/Vaul). Incluye `animate`, `review-animations`, `improve-animations`,
  `find-animation-opportunities`, `apple-design`, `write-swift`,
  `pick-ui-library`.
  Instalación: `npx skills@latest add emilkowalski/skills`

- **pbakaus/impeccable** — Sistema de diseño con 23 comandos (`/impeccable
  audit`, `polish`, `critique`, `animate`, ...) y 59 reglas detectoras de
  anti-patrones de UI generada por IA (gradientes morado-azul, cards
  anidadas, texto gris sobre fondo de color, etc).
  Instalación: `npx impeccable install` (compatible con Claude Code)

Descartado: `dietrichgebert/ponytail` — no es una herramienta de diseño
visual, sino un plugin que hace que el agente escriba código más minimalista
(no aplica al objetivo de mejorar el diseño del sitio).
